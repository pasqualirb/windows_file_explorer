import { ImageElement } from "./imageElement";

class Icon extends ImageElement {

    constructor(icon: string) {
        let a = 1;
        super();
        //this.innerHTML = `<i class="material-icons">${actionButton.icon}</i>`;
    }
}
